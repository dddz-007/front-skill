# 参与改进 front-skill

感谢你帮助改进这个 Skill。最有价值的反馈不是抽象规则，而是 Agent 在真实前端仓库中的具体失败行为。

## 推荐提交内容

- 原始需求或经过脱敏的等价需求；
- 使用的平台与版本，如 Codex、Claude Code、OpenCode、Cursor 或 Qoder；
- 项目技术栈与相关组件库版本；
- Agent 实际采取了什么错误或低效行为；
- 预期行为，以及相关 Diff、日志或截图；
- 问题是否能稳定复现。

提交前请删除密钥、内部地址、客户数据和其他敏感信息。

## 修改原则

- 优先修正已观察到的真实失败，不为假设场景堆叠通用规则；
- 保持 `SKILL.md` 聚焦，把条件性细节放入对应的 `references/`；
- 不绑定某个平台独有的工具名，除非内容明确限定该平台；
- 修改后运行 Skill 结构校验，并检查所有相对链接；
- README 与根目录 Pages 入口的关键事实、安装路径和调用语法必须一致；README 是唯一内容源。

## Pull Request

请在 PR 中简要说明：

1. 观察到了什么问题；
2. 为什么当前规则无法处理；
3. 修改了哪些行为；
4. 如何验证该修改不会扩大无关任务的约束。
